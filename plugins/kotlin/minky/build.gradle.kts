version = "1.0.1"
description = "sends a random picture of minky :3 (zt64's cat)"

aliucord {
    changelog.set("""
        first version
    """.trimIndent())

    excludeFromUpdaterJson.set(false)
}
