version = "1.0.1"
description = "sends a random picture of vennie :3 (vencord mascot)"

aliucord {
    changelog.set("""
        first version
    """.trimIndent())

    excludeFromUpdaterJson.set(false)
}
