version = "1.0.0"
description = "sends a random picture of vennie :3 (aliucord mascot)"

aliucord {
    changelog.set("""
        first version
    """.trimIndent())

    excludeFromUpdaterJson.set(false)
}
